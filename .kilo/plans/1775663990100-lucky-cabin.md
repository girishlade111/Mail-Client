# Webmail Application - Step 11: Full Settings & Personalization

## Current State

Step 10 (Productivity Tools) is complete. The app has:
- **Settings tabs** - Already defined: General, Appearance, Inbox, Accounts, Signatures, Filters, Labels, Organization, Notifications, Shortcuts, Privacy, Integrations, Storage
- **SettingsLayout** - Navigation sidebar with icons
- **ThemeContext** - Light/dark theme support
- **UIContext** - density setting

Most settings tabs have placeholder content only.

## Implementation Plan

### 1. Settings State Management

**Add to ThemeContext:**
```javascript
{
  theme: 'light' | 'dark' | 'system',
  toggleTheme: () => void,
  accentColor: string,
  setAccentColor: (color) => void,
  fontSize: 'small' | 'medium' | 'large',
  setFontSize: (size) => void,
}
```

**Add to UIContext:**
```javascript
{
  language: string,
  setLanguage: (lang) => void,
  timezone: string,
  setTimezone: (tz) => void,
  conversationView: boolean,
  setConversationView: (bool) => void,
  focusedInboxEnabled: boolean,
  setFocusedInboxEnabled: (bool) => void,
  density: 'compact' | 'comfortable' | 'loose',
  setDensity: (density) => void,
  undoDuration: number,
  setUndoDuration: (seconds) => void,
  defaultReplyBehavior: 'reply' | 'replyAll',
  setDefaultReplyBehavior: (behavior) => void,
  readingPanePosition: 'right' | 'bottom' | 'off',
  setReadingPanePosition: (pos) => void,
  previewLines: number,
  setPreviewLines: (lines) => void,
  desktopNotifications: boolean,
  setDesktopNotifications: (bool) => void,
  notificationSound: boolean,
  setNotificationSound: (bool) => void,
  remindersEnabled: boolean,
  setRemindersEnabled: (bool) => void,
  keyboardShortcuts: boolean,
  setKeyboardShortcuts: (bool) => void,
}
```

### 2. General Settings (SettingsGeneral)

**Implement:**
- Language selector (dropdown: English placeholder only for now)
- Timezone selector (dropdown with common timezones)
- Default inbox view (dropdown: Primary/Social/Promotions)
- Reading pane position (radio: Right/Bottom/Off)
- Conversation view toggle (switch)
- Send & archive shortcut toggle (switch)
- Undo send duration (dropdown: 10s/30s/60s)
- Default reply behavior (radio: Reply/Reply all)

### 3. Appearance Settings (SettingsAppearance)

**Enhance:**
- Theme mode (radio: Light/Dark/System) - connect to ThemeContext
- Accent color picker (6 preset colors)
- Font size preference (radio: Small/Medium/Large)
- Density (radio: Compact/Comfortable/Loose) - connect to UIContext
- Message list preview style (radio: Single line/Preview text)

### 4. Inbox Settings (SettingsInbox)

**Implement:**
- Category tabs (switch) - connect to focusedInboxEnabled
- Focused inbox toggle (switch)
- Importance markers (switch)
- Reading pane layout (radio with options)
- Preview line count (dropdown: 1/2/3 lines)
- Swipe actions (placeholder for mobile)

### 5. Accounts Settings (SettingsAccounts)

**Enhance:**
- Already has account list with storage
- Add "Set as default" action per account
- Add "Send test email" placeholder button

### 6. Signatures (SettingsSignatures)

**Implement:**
- List existing signatures per account
- Create new signature button
- Signature editor (textarea with formatting toolbar placeholder)
- Delete signature
- Set default signature per account
- Preview signature

### 7. Filters (SettingsFilters)

**Already implemented:**
- RulesManager component already exists

### 8. Notifications (SettingsNotifications)

**Implement:**
- Desktop notifications toggle
- Notification sound toggle (placeholder)
- Reminder dropdown (30min/1hr/daily/never)
- Per-account notification preference (placeholder)
- Team inbox alerts toggle (placeholder)

### 9. Shortcuts (SettingsShortcuts)

**Implement:**
- Keyboard shortcuts enable/disable toggle
- Shortcut categories display
- Search shortcuts
- View all shortcuts modal (link to KeyboardShortcuts component)

### 10. Labels & Organization (SettingsOrganization/SettingsLabels)

**Already implemented:**
- LabelsManager component
- CategoryManager component

### 11. Privacy & Security (SettingsPrivacy - NEW)

**New Component:**
- External images warning toggle
- Tracking pixel warning toggle (placeholder)
- Blocked senders list management
- Download attachments warning toggle (placeholder)
- Active sessions/device management (placeholder)

### 12. Files to Modify

**Modified:**
1. `src/context/ThemeContext.jsx` - Add more settings
2. `src/context/UIContext.jsx` - Add settings state
3. `src/components/settings/SettingsLayout.jsx` - Implement all settings tabs
4. `src/components/settings/Settings.css` - Complete settings styles

**New:**
1. `src/components/settings/SettingsPrivacy.jsx` - Privacy settings
2. `src/components/settings/SettingsPrivacy.css`

### 13. Implementation Order

1. Expand ThemeContext with more settings
2. Expand UIContext with preferences
3. Implement SettingsGeneral
4. Implement SettingsAppearance
5. Implement SettingsInbox
6. Implement SettingsSignatures
7. Implement SettingsNotifications
8. Implement SettingsShortcuts
9. Create SettingsPrivacy
10. Style all components consistently
11. Test and verify

## Estimated Work

- ThemeContext/UIContext expansion: 1.5 hours
- SettingsGeneral: 1 hour
- SettingsAppearance: 1 hour
- SettingsInbox: 1 hour
- SettingsSignatures: 1.5 hours
- SettingsNotifications: 1 hour
- SettingsShortcuts: 1 hour
- SettingsPrivacy: 1 hour
- CSS styling: 2 hours
- Testing: 1.5 hours

Total: ~11.5 hours