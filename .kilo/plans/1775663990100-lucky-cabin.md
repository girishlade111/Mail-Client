# Webmail Application - Step 6: Organization System (Labels, Folders, Categories)

## Current State

Step 5 is complete with Search & Advanced Filtering. The app has:
- Labels (basic system with colors)
- Folders (inbox, sent, drafts, etc.)
- Categories (Primary, Social, Updates, etc.)
- FilterChips component
- LabelsManager component (basic CRUD)

## Implementation Plan

### 1. Enhanced Labels System

**Enhance LabelsManager.jsx:**
- Create label with name, color, parent (nested labels)
- Rename label
- Delete label (with confirmation, handle emails left behind)
- Change label color (12 predefined colors)
- Nested labels / parent-child hierarchy (up to 3 levels)
- Show/hide label in sidebar toggle
- Reorder labels (drag and drop)

**State to add in MailContext:**
```javascript
{
  labels: [
    { id, name, color, emailCount, parentId?, visible: true, order: 0 }
  ],
  userLabels: [], // user-created labels (separate from system)
}
```

### 2. Labels Integration in MailRow

**Update MailRow.jsx:**
- Display label chips (colored dots + truncated name)
- Max 3 visible, "+N more" overflow for excess
- Hover to show full label names tooltip
- Right-click context menu for label operations

**Update MailRow.css:**
- Label chip styling with color contrast
- Overflow handling (horizontal scroll or +N badge)

### 3. Labels Integration in ThreadView

**Update ThreadView.jsx:**
- Display labels in message header
- Click to add/remove labels (dropdown)
- Show all labels, not limited

### 4. Enhanced Bulk Actions for Labels

**Update BulkActions.jsx:**
- Add "Add label" button with dropdown
- Add "Remove label" option
- Multi-select label picker

### 5. Categories Enhancement

**Update FilterChips.jsx:**
- Add category visibility toggle
- Reorder categories
- Enable/disable focused inbox mode
- Category counts with unread indicator

**Category structure:**
```javascript
{
  id: 'primary',
  name: 'Primary',
  unreadCount: 5,
  visible: true,
  focused: true, // part of focused inbox
}
```

### 6. Folder System Enhancement

**Existing folders (working):**
- inbox, sent, drafts, scheduled, archive, spam, trash, starred, snoozed

**Add folder capabilities:**
- Folder counts (total, unread)
- Drag to folder (in MailRow)
- Nested folders (optional)

### 7. Organization Management Screen

**New Component - OrganizationSettings.jsx:**
- Labels tab: CRUD, reorder, colors, visibility, nesting
- Categories tab: reorder, visibility, focused toggle
- Folders tab: view, reorder, create nested
- Preview: show how labels appear in rows
- Mobile visibility settings

### 8. Context Menu Integration

**Add LabelPicker component:**
- Search/filter labels
- Create new label inline
- List with checkboxes for multi-select
- Color indicators

### 9. Search Integration

**Enhance search filters:**
- label: operator support
- category: filter
- folder: filter

### 10. UX Polish

- Empty labels state
- Label overflow handling (max 10 per email)
- Mobile-friendly label display
- Color contrast for accessibility
- Toast confirmations for label ops

## Files to Modify/Create

1. **src/components/labels/LabelsManager.jsx** - Full CRUD, nesting, visibility
2. **src/components/labels/LabelsManager.css** - Styles
3. **src/components/OrganizationSettings.jsx** - NEW: management screen
4. **src/components/OrganizationSettings.css** - NEW: styles
5. **src/components/LabelPicker.jsx** - NEW: dropdown picker for rows/bulk
6. **src/components/MailRow.jsx** - Label chips display
7. **src/components/MailRow.css** - Label chip styles
8. **src/components/mail/ThreadView.jsx** - Label display in thread
9. **src/components/mail/BulkActions.jsx** - Label operations
10. **src/components/mail/FilterChips.jsx** - Category management
11. **src/context/MailContext.jsx** - Enhanced label state
12. **src/data/mock.js** - Enhanced labels with nesting
13. **src/App.jsx** - Integrate org screen in settings

## State to Add (MailContext)

```javascript
{
  labels: [
    { 
      id: string,
      name: string,
      color: string,
      emailCount: number,
      parentId: string | null,
      visible: boolean,
      order: number,
      nested: Label[] // children
    }
  ],
  categories: [
    {
      id: string,
      name: string,
      visible: boolean,
      focused: boolean,
      unreadCount: number,
      order: number
    }
  ],
  focusedInboxEnabled: boolean,
}
```

## Components to Create

### LabelPicker
- Dropdown with search
- Multi-select checkboxes
- Inline create
- Color indicators

### OrganizationSettings
- Tabbed interface (Labels, Categories, Folders)
- Drag-drop reorder
- Preview panel
- Mobile settings

## Implementation Order

1. Enhance labels in MailContext
2. Enhance LabelsManager with full CRUD
3. Add LabelPicker component  
4. Update MailRow with label chips
5. Update ThreadView with labels
6. Enhance BulkActions for labels
7. Add category management to FilterChips
8. Create OrganizationSettings screen
9. Integrate with settings
10. Test and polish

## Estimated Work

- Labels CRUD: 1.5 hours
- LabelPicker: 1 hour
- MailRow labels: 1 hour
- ThreadView labels: 0.5 hours
- BulkActions labels: 0.5 hours
- Categories: 1 hour
- OrganizationSettings: 2 hours
- Integration: 1 hour
- Polish: 1.5 hours

Total: ~10 hours